spring:
  application:
    name: microservice-produtos
  datasource:
    url: jdbc:postgresql://localhost:5555/ms_produtos
    username: postgres
    password: postgres
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
server:
  port: 8084