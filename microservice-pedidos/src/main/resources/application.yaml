spring:
  application:
    name: microservice-pedidos
  datasource:
    url: jdbc:postgresql://localhost:5555/ms_pedidos
    username: postgres
    password: postgres
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
server:
  port: 8083